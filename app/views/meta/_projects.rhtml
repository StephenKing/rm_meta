<% if project.description.present? %>
  <% description = strip_tags(textilizable(project.description, :project => project)) %>
  <% meta_description(description.gsub(/^(.{255}[^\n\r]*).*$/m, '\1...').strip) %>
  <% meta_keywords(extract_keywords(description)) %>
<% end %>
