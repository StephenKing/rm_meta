<% if news.summary.present? && news.description.present? && (!meta_description? || !meta_keywords?) %>
  <% description = strip_textile(news.description, news.project) %>
  <% meta_description(news.summary) unless meta_description? %>
  <% meta_keywords(extract_keywords(description)) unless meta_keywords? %>
<% end %>
<!-- TODO: Open Graph "article" + Twitter Card "summary" -->
